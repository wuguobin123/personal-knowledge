ARG NODE_IMAGE=node:20-alpine
FROM ${NODE_IMAGE}

WORKDIR /app

ENV NODE_ENV=development

COPY package.json package-lock.json ./
COPY prisma ./prisma
COPY prisma.config.ts ./

RUN npm ci

EXPOSE 3000

CMD ["sh", "-c", "npx prisma generate && npm run db:push && npm run dev -- --webpack"]
